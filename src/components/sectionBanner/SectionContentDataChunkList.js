import styled from "styled-components"
import React from "react"
import * as colors from "../../utils/colors"
import PropTypes from "prop-types"
import { breakpoint } from "../../utils/breakpoints"

const SectionContentDataChunkList = () => {
  return (
    <SectionContentDataChunkListWrapper></SectionContentDataChunkListWrapper>
  )
}

const SectionContentDataChunkListWrapper = styled.span`
  background-color: pink;
`
export default SectionContentDataChunkList
